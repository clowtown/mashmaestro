[pytest]
addopts = -ra -v --cache-clear -n auto
log_cli = 1
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s (%(filename)s:%(lineno)s)
log_cli_date_format=%Y-%m-%d %H:%M:%S

junit_family = xunit2
filterwarnings =
    ignore:Could not import the lzma module:UserWarning
